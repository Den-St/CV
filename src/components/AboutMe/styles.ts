import { styled } from "styled-components";

export const AboutMeItem = styled.p`
    font-size: 16px;
    font-weight: 100;
    margin:0;
`;

export const AboutMeContainer = styled.div`
    display:flex;
    flex-direction:column;
`;